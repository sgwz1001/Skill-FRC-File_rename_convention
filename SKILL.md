---

name: 文件重命名规范
description: "新建 / 另存 / 重命名文件时，将文件名统一规范为「文件名-文件类型-时间-v版本号&备注.扩展名」格式，并依据跨天、大改、同日小幅规则自动推算下一个版本号。当用户说“按命名规范建文件”“用文件重命名规则”“按我们的命名法存一下”，或本 skill 已启用且涉及创建、保存、重命名文件时，使用此 skill。"
agent_created: true
---

# 文件命名规范（新建文件重命名）

## 用途

任何"新建 / 另存为 / 重命名 / 保存"动作，都按统一规范生成文件名，并自动维护版本号，
避免手动记忆 v 几、日期是否更新。

## 标准结构

```
文件名-文件类型-时间-v版本号&备注.扩展名
```

## 版本递增规则（核心，务必遵守）

- **首次创建**：目录中无同名主题 → `v1`，日期取当天。
- **跨天再改**：与最新版本日期不同（哪怕只改一点点）→ 主版本 +1（v1→v2），日期更新为当天。
- **大篇幅重构 / 大改**：主版本 +1（v2→v3）；主版本进位时次版本清零（v1.3 跨天变 v2）。
- **同一天多次小幅修改**：次版本 +0.1（v1→v1.1→v1.2），日期不变。

日期格式固定 `YYYYMMDD`（如 `20260801`）。「文件类型」是中文类型描述（如 合同 / 数据表格 /
效果预览网页），与扩展名（docx / xlsx / html）分开。

## 工作流

当需创建 / 保存 / 重命名文件并套用本规范时：

1. 向用户确认或推断四个要素：**文件名主体**、**文件类型描述**、**扩展名**、**备注**（备注可空）。
2. 运行版本计算脚本，让它扫描目标目录并给出建议文件名：

   ```bash
   python "<skill_dir>/scripts/suggest_name.py" \
     --theme "平台首页效果预览" \
     --type "效果预览网页" \
     --ext html \
     --note "微调了UI布局" \
     --dir "<目标目录>" \
     [--major]            # 大篇幅重构 / 大改时加此参数
     [--date 20260801]    # 需要指定日期时
   ```

   - `<skill_dir>` 为本 skill 所在目录（即包含 SKILL.md 的目录）。
   - 脚本输出即最终文件名；加 `--json` 可查看版本拆解。
3. 用脚本返回的文件名创建 / 保存 / 重命名文件。
4. 若用户要求“大改 / 重构 / 重做”，在命令中加 `--major` 强制主版本进位。

## 判定要点

- 拿不准是否“大改”时，默认按**同日小幅**处理（次版本 +0.1）；仅当用户明确说大改/重构，
  或文件日期与最新版本不同，才主版本 +1。
- 备注中的 `\ / : * ? " < > |` 等字符会被脚本自动清洗为空格，无需手动处理。

## 资源

- `scripts/suggest_name.py` — 确定性版本计算引擎。扫描目录、解析最新版本、按规则输出下一文件名。
- `references/examples.md` — 规范详解、版本规则表与用户原始案例，需要时加载参考。
