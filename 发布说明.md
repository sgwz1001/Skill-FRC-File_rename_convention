# 文件重命名规范 v1.0.0

首个公开发布版本。

## 这个 Skill 做什么

把「新建 / 另存 / 重命名」动作统一成规范文件名 `文件名-文件类型-时间-v版本号&备注.扩展名`，
并自动按「跨天进位 / 大改进位 / 同日小幅 +0.1」规则维护版本号，免去记忆 v 几、日期要不要更新。

## 包含内容

- `SKILL.md`：触发词与工作流
- `scripts/suggest_name.py`：确定性版本计算引擎
- `references/examples.md`：规范详解与版本规则表

## 安装方法

将本仓库（或下方附件 zip）整体复制到：

- 用户级：`C:\Users\<你的用户名>\.workbuddy\skills\file-rename-convention\`
- 项目级：`<工作目录>\.workbuddy\skills\file-rename-convention\`

刷新对话后，说「按命名规范建一个文件」即可触发。

## 用法示例

```bash
python "<skill目录>/scripts/suggest_name.py" \
  --theme "平台首页效果预览" --type "效果预览网页" --ext html \
  --note "微调了UI布局" --dir "<目标目录>"
```

## Release 附件

`file-rename-convention.zip`：可直接解压使用的完整 skill 文件夹。
